#include <stdio.h>

int main() {
    char nomes[3][50];
    scanf("%s", nomes[0]);
    scanf("%s", nomes[1]);
    scanf("%s", nomes[2]);

    printf("%s \n", nomes[0]);
    printf("%s \n", nomes[1]);
    printf("%s \n", nomes[2]);

    return 0;
}